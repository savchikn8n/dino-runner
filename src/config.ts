/**
 * Игровые константы в одном месте — крути отсюда баланс и «дух оригинала».
 * Все величины — в логических пикселях (мир рисуется в фиксированном разрешении,
 * см. core/renderer.ts) и в пикселях-в-секунду / в-секунду^2 для физики.
 */

export const VIEW = {
  /** Логическое разрешение мира. Масштабируется под экран целочисленно. */
  width: 800,
  height: 300,
  /** Линия земли (верх «асфальта») от верха кадра. */
  groundY: 252,
} as const;

export const SPEED = {
  /** Стартовая скорость прокрутки мира, px/сек. */
  start: 320,
  /** Потолок скорости, px/сек. */
  max: 820,
  /** Прирост скорости за секунду игры, px/сек^2. */
  accel: 7,
} as const;

export const DINO = {
  x: 56,
  width: 44,
  height: 47,
  /** Импульс прыжка вверх, px/сек (отрицательный = вверх). */
  jumpVelocity: -680,
  /** Гравитация, px/сек^2. */
  gravity: 2100,
  /** Доп. гравитация при отпускании прыжка раньше (вариативная высота). */
  fastFallGravity: 3400,
  /** Уменьшение хитбокса со всех сторон для «честных» столкновений, px. */
  hitboxInset: 6,
  /** Кадров анимации бега в секунду. */
  runFps: 10,
} as const;

export const OBSTACLE = {
  /** Минимальный/максимальный зазор между препятствиями, px (при старте). */
  minGap: 280,
  maxGap: 520,
  /** Во сколько раз сжимается зазор на максимальной скорости. */
  gapSpeedFactor: 0.55,
} as const;

export const SCORE = {
  /** Очков за пройденный логический пиксель. */
  perPixel: 0.025,
  /** Каждые N очков — звуковой «бип» и вспышка. */
  milestone: 100,
} as const;

export const WEATHER = {
  /** Сколько секунд держится одно состояние погоды (мин/макс). */
  minDuration: 14,
  maxDuration: 26,
} as const;

export const LINKS = {
  channel: "https://t.me/savchiksasha",
  developer: "https://t.me/savchiksasha",
} as const;
