/**
 * Игровые константы в одном месте — крути отсюда баланс и «дух оригинала».
 * Размеры мира (ширина/высота/линия земли) — в core/viewport.ts, т.к. высота
 * зависит от экрана. Здесь — физика и баланс в логических единицах.
 */

export const SPEED = {
  /** Стартовая скорость прокрутки мира, px/сек. */
  start: 300,
  /** Потолок скорости, px/сек. */
  max: 760,
  /** Прирост скорости за секунду игры, px/сек^2. */
  accel: 28,
} as const;

export const DINO = {
  x: 48,
  /** Целевая высота спрайта в логических px (ширина — из пропорций матрицы). */
  height: 48,
  /** Импульс прыжка вверх, px/сек (отрицательный = вверх). */
  jumpVelocity: -700,
  /** Гравитация, px/сек^2. */
  gravity: 2200,
  /** Доп. гравитация при отпускании прыжка раньше (вариативная высота). */
  fastFallGravity: 3600,
  /** Уменьшение хитбокса со всех сторон для «честных» столкновений, px. */
  hitboxInset: 5,
  /** Кадров анимации бега в секунду. */
  runFps: 12,
} as const;

/**
 * Зазоры между препятствиями задаются во ВРЕМЕНИ, а не в пикселях. Так гарантируется,
 * что между кактусами всегда хватает места приземлиться и прыгнуть снова — на любой
 * скорости (в пикселях зазор растёт вместе со скоростью).
 *
 * Время прыжка ≈ 2*|jumpVelocity|/gravity. Минимальный зазор = это время + запас
 * на приземление и реакцию.
 */
export const OBSTACLE = {
  /** Размер «пикселя» матрицы кактуса, логические px. */
  pixel: 3,
  /** Запас (в секундах) сверх времени прыжка для минимального зазора. */
  minGapBuffer: 0.45,
  /** Дополнительный случайный разброс зазора, сек. */
  maxGapExtra: 1.3,
  /** Пауза перед первым кактусом, сек. */
  startDelay: 1.1,
} as const;

export const SCORE = {
  /** Очков за пройденный логический пиксель. */
  perPixel: 0.025,
  /** Каждые N очков — звуковой «бип». */
  milestone: 100,
} as const;

export const WEATHER = {
  /** Сколько секунд держится одно состояние погоды (мин/макс). */
  minDuration: 14,
  maxDuration: 26,
} as const;

export const LINKS = {
  channel: "https://t.me/savchiksasha",
} as const;
