/**
 * Рантайм-вьюпорт игрового мира. Ширина мира фиксирована (детерминированный
 * геймплей и масштаб спрайтов), а ВЫСОТА подстраивается под экран: мир всегда
 * вписан по ширине и заполняет экран по вертикали, линия земли прижата к низу.
 *
 * Так раннер корректно живёт и в портретном Telegram, и в ландшафте: сверху
 * получается «небо», снизу — трасса. Значения обновляет Renderer.resize().
 */

export const view = {
  /** Логическая ширина мира (фиксирована). */
  width: 540,
  /** Логическая высота мира (зависит от соотношения сторон экрана). */
  height: 960,
  /** Y линии земли (верх «асфальта»). Прижата к низу экрана. */
  groundY: 820,
};

/** Доля высоты, отводимая под землю снизу (между линией земли и низом экрана). */
export const GROUND_BOTTOM_RATIO = 0.12;
