/**
 * Рантайм-вьюпорт игрового мира. Ширина мира фиксирована (детерминированный
 * геймплей и масштаб спрайтов), а ВЫСОТА подстраивается под экран. Линия земли
 * проходит ближе к середине экрана (удобно: палец тапает снизу, не перекрывая
 * динозавра).
 *
 * topInset — безопасный отступ сверху в ЛОГИЧЕСКИХ px (чёлка устройства + шапка
 * Telegram), чтобы счёт и солнце не уезжали под системные элементы.
 *
 * Значения обновляет Renderer.resize().
 */

export const view = {
  /** Логическая ширина мира (фиксирована). Меньше число → крупнее всё на экране. */
  width: 360,
  /** Логическая высота мира (зависит от соотношения сторон экрана). */
  height: 640,
  /** Y линии земли (верх «асфальта»). */
  groundY: 333,
  /** Безопасный отступ сверху в логических px (чёлка + шапка Telegram). */
  topInset: 0,
};

/** Доля высоты от верха до линии земли (≈ середина экрана). */
export const GROUND_Y_RATIO = 0.52;
